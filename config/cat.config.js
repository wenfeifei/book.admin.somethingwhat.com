/**
 * 项目配置
 */
const catconfig = {
  /**
   * 项目名称，页面title等使用
   */
  projectName: '喵喵看书后台',
  /**
   * 用作项目某些配置（如 window.localStorage）key的前缀
   */
  projectPrefix: 'cat-book',
  /**
   * 数据接口的代理
   */
  proxy: 'https://book.somethingwhat.com', // http://localhost:11888
};

export default catconfig;